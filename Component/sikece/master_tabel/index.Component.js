import dynamic from 'next/dynamic';

export default class IndexMasterTabel extends React.Component {
    state = {
        
    }

    render() {
        return (
            <React.Fragment>
                Master Tabel
            </React.Fragment>
        )
    }
}