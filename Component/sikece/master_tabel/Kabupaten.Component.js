import dynamic from 'next/dynamic';

export default class Kabupaten extends React.Component {
    state = {
        data: "Kabupaten"
    }

    render() {
        return (
            <div>Kabupaten</div>
        )
    }
}